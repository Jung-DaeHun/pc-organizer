node_modules/
out/
dist/
release/
*.log
.DS_Store
.vite/

# 개인 설정은 공유하지 않는다 (.claude/settings.json 은 팀 공유용이라 커밋한다)
.claude/settings.local.json

# 진행 메모는 로컬에만 둔다
PROGRESS.md

# 코드 서명 인증서와 개인 키는 저장소에 절대 넣지 않는다 (서명은 WIN_CSC_LINK / CSC_KEY_PASSWORD 환경 변수로)
*.pfx
*.p12
*.key
